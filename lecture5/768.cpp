#include<bits/stdc++.h>

using namespace std;

int main(){
    string s1, s2;
    getline(cin, s1);
    getline(cin, s2);

    int len1 = s1.size(), len2 = s2.size(), len = min(len1, len2);

    for (int i = 0; i < len; i++){

        
    }

        return 0;
}